/**
 * Corner-radius scale. `pill` (999) renders a perfect circle/oval regardless
 * of the component's height — use for chips, FABs, and tag pills.
 */
export const radius = {
  sm: 6,
  md: 10,
  lg: 16,
  pill: 999,
} as const;
